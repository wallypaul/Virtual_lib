// Use config folders to hold your variable
// This allows you to easily add config varibles to your project
// and keep them separate from the rest of your code.
const config = {
    apiUrl: 'http://localhost:8000/api/',
    debugMode: true,
  };
  
  export default config;